import 'package:flutter/material.dart';

class TouchPoint {
   Paint paint;
   double radius;
   Offset points;

  TouchPoint({
    required this.paint,
    required this.radius,
    required this.points,
  });
}
